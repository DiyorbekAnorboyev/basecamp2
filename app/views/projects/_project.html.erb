<div class="card mt-3 mx-3 w-50">
  <div class="card-body">
    <h1 class="card-title mb-5"><strong><%= project.name %></strong></h1>
    <p class="card-text bg-light border border-light"><%= project.description  %></p>
    <div class="d-flex justify-content-between">
      <div>
        <%= link_to "Show this project", project, class:'btn btn-primary' %>
      </div>
      <div>
        <h5 class="text-end"><strong>Created:</strong> <%= current_user.email%></h5>
      </div>
    </div>
  </div>
</div>